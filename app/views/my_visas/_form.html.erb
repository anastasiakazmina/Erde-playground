<%= form_with(model: my_visa, local: true) do |form| %>
  <% if my_visa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(my_visa.errors.count, "error") %> prohibited this my_visa from being saved:</h2>

      <ul>
      <% my_visa.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_field :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
